# Secrets. GitHub App private keys download straight into the repo root, so this is a
# whole-extension ban rather than one filename.
*.pem
*.key
.env
.env.*
!.env.example
!.env.*.example

# Generated by scripts/tunnels.mjs. Names your domain and points at your credentials file.
.cloudflared/

# OS
.DS_Store
